#include <iostream>
#include <cmath>
using namespace std;

int main()
{
    float diameter1, diameter2, price1, price2;
    cout << "Enter the diameter (in inches) of the first pizza: ";
    cin >> diameter1;
    cout << "Enter the price (in USD) of the first pizza: ";
    cin >> price1;
    cout << "Enter the diameter (in inches) of the second pizza: ";
    cin >> diameter2;
    cout << "Enter the price (in USD) of the second pizza: ";
    cin >> price2;
    return 0;
}